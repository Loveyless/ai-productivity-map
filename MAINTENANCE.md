# 自动维护策略

这个目录以准确性和任务价值优先，不追求工具数量。自动维护允许一次运行产生 **0 个变更**。

## 运行周期

- 内容维护每 48 小时执行一次，锚定在 `Asia/Shanghai 01:30`。
- 调度器每天在对应时刻触发轻量门控；距离上次成功维护不足 47 小时时静默退出。
- 失败运行不更新时间门控，下一天同一时刻自动重试。

## 信息来源

每次维护至少使用以下两类来源：

1. X Search：发现近期产品发布、官方更新和可能过时的目录信息。
2. 官方一手来源：产品官网、官方文档、更新日志、官方博客、官方 GitHub 仓库或官方账号。

X 帖子只用于发现线索。没有官方一手来源交叉验证的内容，不得写入目录。

## 允许自动修改的范围

自动任务只维护目录内容、品牌元数据和本地品牌资源：

- 修正现有工具的官方入口、用途、访问边界和平台信息；
- 添加满足收录标准、已经公开可用的新工具；
- 更新经官网验证的品牌主题色和本地图标；
- 更新与这些目录变更直接相关的说明文档。

自动任务不得修改站点架构、依赖、工作流或测试代码。若发现管线缺陷，只报告，不自动修复代码。

单次运行最多：

- 新增 3 个工具；
- 修改 5 个现有工具记录。

这些是安全上限，不是配额。不能为了达到数量而降低收录标准。

## 收录和删除边界

新增工具必须同时满足：

- 有明确、可复述的真实任务价值；
- 有稳定的官方入口，且已公开可用，不是纯候补名单或传闻；
- 相比现有条目有明显差异，不是缺乏独特能力的简单套壳；
- 访问、平台、定价或地区限制能够被官方来源验证。

自动任务不直接删除工具。若工具疑似下线、被收购或入口失效，应在运行报告中列为人工复核项；单次抓取失败或 X 传闻都不能作为删除依据。

## 品牌资源

- 图标和 Logo 优先来自产品官网声明的 icon、manifest 或官网引用的官方 CDN；页面、Manifest、图标和重定向只能落在该工具现有证据形成的已批准 host 集合，新跨域 CDN host 必须先人工核验，HTML `<base>` 不得扩大信任边界。
- 所有图标转换为仓库内的 96×96 PNG，不使用热链、第三方 favicon 聚合服务或跟踪资源。
- 联网抓取只允许 HTTPS 与公网地址；每次重定向都重新校验，并把审核后的 DNS 地址绑定到实际 TLS 连接。
- 每个工具最多尝试 12 个图标候选，并对 DNS、TLS、重定向、body 和可终止图片解码 Worker 共用 45 秒绝对墙钟预算；ICO 帧数、编码大小和解码尺寸均有硬上限。
- 自动同步一律拒绝 SVG；若确需采用 SVG 品牌素材，只能在独立人工安全审查后离线栅格化为 PNG 再入库。
- 新图标使用 ID 绑定的内容寻址路径并记录完整 SHA-256；发布拒绝 symlink、使用不可覆盖写入和协作单写者锁，再在写入 catalog 前复核最初读取的基线，rename 后执行目录 fsync 和 readback。该检查不是不合作外部编辑器可参与的通用文件系统原子 CAS，仍须依赖干净工作区和单实例调度。
- 主题色只接受官网 `theme-color`、manifest 或同等一手元数据；无法确认时保留中性回退，不凭印象猜测。
- `brandIconReviewedAt` 与 `brandThemeColorReviewedAt` 分别表示对应证据被采纳或变更的日期；`reviewedAt` 只在产品内容确实被复核时更新。

## 发布质量门

每次发生变更后必须依次通过：

```bash
npm ci
npm test
npm run validate:catalog
npm run check
npm run build
npm audit --audit-level=high
```

`npm run sync:brands -- --check` 是严格联网审计，不是离线发布门：任何官方页面访问受限、超时或来源不确定都会以非零退出，并报告为 `inconclusive`，不能写成 passed。自动任务必须保留已有证据并记录该审计结果；只有在没有 inconclusive 且没有 proposed changes 时，才能称严格联网审计通过。

还必须检查：

- 工作区开始时必须干净，并先执行 `git pull --ff-only`；
- 不包含凭据、Token、私钥或与目录维护无关的文件；
- 图标均为本地常规 PNG 文件；
- 变更范围符合本文件的单次上限；
- 只有全部质量门通过后才能提交并推送 `main`；
- 禁止强推，禁止跳过失败测试，禁止把 warning 冒充 passed。

没有内容变更时不创建空提交。推送后应核验 GitHub Actions 和 GitHub Pages；部署失败时报告失败，不覆盖或强推线上版本。

## 运行报告

每次实际维护运行应报告：

- X Search 查询范围和候选数量；
- 采用与拒绝的候选及一手证据；
- 修改的工具、品牌资源和提交；
- 测试、构建、CI、Pages 状态；
- 需要人工复核但未自动修改的事项。
